name: Twitter Data Scraper

on:
  # 手动触发
  workflow_dispatch:
    inputs:
      batch_size:
        description: 'Batch size (random N users from database)'
        required: false
        default: '20'
      frequency_group:
        description: 'User frequency group to scrape'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - high     # 高频用户 (每2小时)
          - medium   # 中频用户 (每4小时)
          - low      # 低频用户 (每6小时)

  # 自动定时任务（北京时间 8-24点）
  schedule:
    # 高频用户：每2小时 (北京时间 8, 10, 12, 14, 16, 18, 20, 22点)
    - cron: '0 0,2,4,6,8,10,12,14 * * *'
    # 中频用户：每4小时 (北京时间 8, 12, 16, 20点)
    - cron: '30 0,4,8,12 * * *'
    # 低频用户：每6小时 (北京时间 8, 14, 20点)
    - cron: '45 0,6,12 * * *'

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      HEADLESS: true
      WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
      TWITTER_COOKIES_JSON: ${{ secrets.TWITTER_COOKIES_JSON }}
      EXT_TWEET_PATH: ./TwExport/2.6.0_0
      EXT_FOLLOWER_PATH: ./Twitter Export Follower/3.8.0_0

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Chrome
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run scraper
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          SCHEDULE_CRON: ${{ github.event.schedule }}
          INPUT_FREQ: ${{ github.event.inputs.frequency_group || 'all' }}
          INPUT_BATCH: ${{ github.event.inputs.batch_size || '' }}
        run: |
          # 定时任务根据触发的 cron 表达式确定频率组（避免执行延迟导致的时间漂移）
          if [ "$EVENT_NAME" = "schedule" ]; then
            # 从 cron 表达式中提取分钟数（格式: "M H * * *"）
            CRON_MINUTE=$(echo "$SCHEDULE_CRON" | awk '{print $1}')
            case $CRON_MINUTE in
              0)  FREQ="high";   BATCH_SIZE=15;;  # 高频用户：采集频繁，每次少量
              30) FREQ="medium"; BATCH_SIZE=25;;  # 中频用户：适中
              45) FREQ="low";    BATCH_SIZE=30;;  # 低频用户：采集少，每次多量
              *)  FREQ="all";    BATCH_SIZE=20;;
            esac
            echo "📅 触发的 cron: $SCHEDULE_CRON (分钟=$CRON_MINUTE)"
          else
            FREQ="$INPUT_FREQ"
            BATCH_SIZE="${INPUT_BATCH:-20}"  # 手动触发使用输入值或默认20
          fi
          echo "🚀 频率组: $FREQ, 批次大小: $BATCH_SIZE"
          npm run batch -- --batch-size=$BATCH_SIZE --frequency=$FREQ

      - name: Notify on failure
        if: failure() && env.WEBHOOK_URL != ''
        run: |
          curl -X POST ${{ env.WEBHOOK_URL }} \
            -H 'Content-Type: application/json' \
            -d "{\"text\":\"❌ Twitter scraper failed! Run #${{ github.run_number }}\"}" || true
